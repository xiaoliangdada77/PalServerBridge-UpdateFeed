---
version: 1.5.11
release_date: 2026-06-22
title: v1.5.11 物品与帕鲁策略限制版
summary: 新增物品使用/进食/消耗/装备限制与帕鲁出战限制；抽取共享策略规则引擎，支持 deny/require、等级与属性强化条件、私聊提示和测试接口；修复策略热重载、出战拦截、装备拦截与本地化属性名归一化问题
---

# PalServerBridge 更新说明

这是 `PalServerBridge` 的公开更新公告源，保留完整历史更新说明链，内容与后台项目 DLL 公告保持同步。

插件启动时只读取顶部元数据识别当前最新版本，正文用于展示完整版本历史。

## 当前版本

- 当前版本：`1.5.11`
- 发布时间：`2026-06-22`
- 更新摘要：新增物品使用/进食/消耗/装备限制与帕鲁出战限制；抽取共享策略规则引擎，支持 deny/require、等级与属性强化条件、私聊提示和测试接口；修复策略热重载、出战拦截、装备拦截与本地化属性名归一化问题
- 公开更新源地址：`https://raw.githubusercontent.com/xiaoliangdada77/PalServerBridge-UpdateFeed/master/UPDATE.md`

## 完整更新历史

### v1.5.11 物品与帕鲁策略限制版

- 更新定位：服务器侧物品/帕鲁使用策略限制、条件门槛和玩家提示能力
- 适用对象：需要限制指定物品使用、装备、进食，或限制指定帕鲁出战的服主与后台开发者
- 更新阶段：策略规则系统上线与热重载修复阶段

这一版新增服务器侧策略限制系统，用于按物品 ID 或帕鲁 CharacterID 配置禁用规则或条件规则。物品策略覆盖使用、进食、消耗和装备；帕鲁策略聚焦出战限制。规则引擎已抽取为共享模块，避免物品和帕鲁各自维护孤立逻辑。

#### 新增能力

- 新增 `item_policy`，支持限制 `use`、`eat`、`consume`、`equip` 动作。
- 新增 `pal_policy`，支持限制 `deploy` 出战动作。
- 新增 `/api/item_policy/get`、`/api/item_policy/set`、`/api/item_policy/test`。
- 新增 `/api/pal_policy/get`、`/api/pal_policy/set`、`/api/pal_policy/test`。
- `/api/capabilities` 新增 `item_policy` 与 `pal_policy` 能力标记，方便外部后台做功能门控。
- `mode=deny` 可直接禁用指定物品或帕鲁；`mode=require` 可按条件限制使用。
- 条件限制支持 `requirements.min_level` 和 `requirements.status_points`。
- 属性强化条件使用标准英文键：`hp`、`sp`、`attack`、`work`、`weight`。

#### 行为与提示

- 拦截成功后会阻止原始游戏操作，避免只提示但仍然生效。
- 玩家提示接入私聊系统，不广播打扰其他玩家。
- 自定义 `message` 为可选字段；未配置时使用内置 i18n 文本。
- 物品或帕鲁本地化名称会自动拼接在提示文本最前方，例如“攻击吊坠 当前被服务器禁用”。
- 同一条 `require` 规则中未满足的条件会一次性全部列出，不再只提示第一个缺失条件。
- 测试接口会返回命中规则、阻止原因、最终提示文本和玩家上下文，便于后台调试。

#### 修复与收口

- 修复食物拦截因只读回调无法阻止原函数的问题。
- 修复装备拖动、右键快捷装备等路径未触发物品策略的问题。
- 修复私聊通知使用玩家 UID 时参数解析失败的问题。
- 修复玩家属性强化读取到本地化名称后无法匹配 `attack`、`hp` 等英文配置键的问题。
- 修复帕鲁策略热重载后旧规则残留、初始化时误伤已加载帕鲁、以及错误拦截帕鲁选择流程的问题。
- 移除未稳定验证的帕鲁据点槽位限制动作，`pal_policy` 当前只保留 `deploy`。

#### 接入说明

- 物品策略配置入口为 `item_policy.rules[]`，目标字段为 `item_id` 或 `item_ids`。
- 帕鲁策略配置入口为 `pal_policy.rules[]`，目标字段为 `pal_id` 或 `pal_ids`；兼容 `character_id` / `character_ids` 输入。
- `actions` 省略时匹配该策略支持的全部动作；建议生产环境显式填写动作，降低误配风险。
- `message` 只写限制原因即可，不需要手动拼物品名或帕鲁名。
- 外部后台建议优先调用 `test` 接口验证配置，再调用 `set` 保存正式策略。

#### 版本备注

- 当前对外同步版本：`v1.5.11`
- 这一版覆盖 2026-06-22 的物品策略、帕鲁出战策略、共享规则引擎、私聊提示、条件拼接和属性强化归一化修复。

### v1.5.10 物品转移数量上限与装备恢复修复版

- 更新定位：网页仓库取回、物品发放、礼包发放、物品删除和失败回滚链路修复
- 适用对象：使用商城后台、网页仓库、奖励暂存、批量发物品、金币取回和装备类物品转移的服主与开发者
- 更新阶段：物品链路行为收口阶段

这一版集中处理 1.5.9 之后暴露出的物品转移问题：大数量金币取回被 `Count: 参数无效` 拦截、后台日志无法明确判断失败原因、`gift -> item` 模板路径仍残留独立 100000 上限、删除路由也存在独立 100000 上限，以及装备类物品在自动装备槽位后恢复状态失败导致网页仓库误判和回滚风险。

#### 重要调整

- 物品发放、礼包发放、底层背包 AddItem 和物品删除统一使用共享上限 `int32 / 2`，当前最大值为 `1073741823`。
- 新增共享 `GameLimits` 与 `JsonValidation`，移除 `GameApiModule`、`TemplateEngine`、`PalPlayerInventoryWrapper`、HTTP 删除路由中的孤立 100000 常量和重复 Count 校验。
- `/api/execute` 的 `item`、`gift.items[]`，以及 `/api/delete_item` 的 `amount` 都改为共享 JSON 正整数校验。
- `Count` / `amount` 校验失败时返回明确原因，例如允许范围、当前类型、当前值或读取失败原因，不再只返回 `Count: 参数无效`。
- 更新物品模板和删除物品 API 文档中的数量上限说明，避免文档继续误导为 100000。

#### 这版修复了什么

- 修复网页仓库取回大数量金币时，后台显示 `Count: 参数无效` 且游戏端没有进入后续发放日志的问题。实际原因是 gift 模板层在进入 `GiveItemDetailed` 前提前拦截。
- 修复 `/api/delete_item` 删除路由仍然走 100000 上限的问题，普通删除和精确槽位删除都已切换到共享上限。
- 修复装备类物品取回后进入装备槽位时，恢复字段与装备槽位数据不匹配导致恢复失败、误判发放失败和触发回滚的问题。
- 对动态物品恢复字段做兼容收口：保留有效动态字段，忽略过期或当前物品不支持的恢复字段，避免默认腐化值、子弹数等无关字段污染装备类物品恢复。
- 物品发放失败结果继续返回 slot、restore、max_count、error 等结构化信息，便于外部后台准确记录失败原因。

#### 接入说明

- 如果你在网页仓库中取回金币、普通物品或装备类物品，建议更新到这一版后重新测试大数量取回和装备取回。
- 外部后台不再需要因为插件 100000 上限对大数量金币做额外分片；但仍建议业务层根据游戏内物品栈、玩家背包空间和风控策略自行限制。
- 如果仍然出现取回失败，请优先查看返回的 `message`、`error`、`max_count`、`slot_tracking` 和 `restore` 字段，而不是只看顶层成功/失败。
- 这一版不改变 `spawn_pal` 生成管线，不重新引入踢人接口，也不包含等级上限突破能力。

#### 版本备注

- 当前对外同步版本：`v1.5.10`
- 这一版覆盖 2026-06-21 的装备类物品恢复、物品发放数量上限、模板 Count 校验、删除路由 amount 上限和错误返回说明修复。

### v1.5.9 spawn_pal 原生 AI 生成稳定版

- 更新定位：`spawn_pal` 世界生成稳定性重构、原生 AI 初始化收口与队列节奏配置化
- 适用对象：使用 `/api/execute`、`spawn_pal`、商城刷怪、活动生成、自动化生成脚本和需要批量生成世界帕鲁的服主与开发者
- 更新阶段：生成管线重构收口阶段

这一版集中处理 1.5.8 之后继续暴露的 `spawn_pal` 问题：多数量生成有时直接崩溃、生成后没有攻击 AI/游荡 AI、原生生成路径有时返回无 handle、部分方案会重复生成或结果不可控。经过多轮验证后，1.5.9 不再保留多 fallback、多后处理、多重复 tick 的生成链路，而是收口为单一原生 spawner delegate 管线。

#### 重要调整

- `spawn_pal` 改为使用隐藏的 `BP_PalSpawner_Standard_C` 上下文和原生 `Spawn Delegate` 完成 AI 初始化。
- 生成结果必须带原生 AI。无法创建 spawner 上下文、无法绑定原生 delegate、无法写入 `IndividualHandleList` 等情况会直接失败，不再降级生成无 AI 帕鲁。
- 移除不稳定的直接 BP 生成、Network Actor 生成、CharacterManager 生成、Native outside spawn request、raw handle fallback、强制 AI 激活和反射式 AI 后处理路径。
- 移除人工请求节奏限制和 spawner context tick override，避免同一功能里存在多套互相影响的节奏控制。
- `GivePal` 与 `spawn_pal` 仍然保持职责分离：`GivePal` 是发放到玩家容器，`spawn_pal` 是世界坐标生成。

#### 这版修复了什么

- 修复 `count > 1` 批量生成后几秒内服务端窗口直接关闭、无日志崩溃的问题。
- 修复生成成功但帕鲁没有攻击 AI、没有游荡 AI、只站在原地的问题。
- 修复部分生成路径返回成功但实际没有生成、返回 no handles、或同一请求生成两份的问题。
- 修复中途 finalization、actor ready hook、参数初始化回调等时序方案带来的不稳定崩溃风险。
- 批量生成继续通过游戏线程队列执行，HTTP 请求线程只负责入队，避免跨线程直接触碰游戏对象。
- 队列生命周期在模块初始化和关闭时重置，减少重载、关闭或残留请求影响下一轮运行。
- 生成完成后的确认逻辑与 AI 初始化解耦，减少 finalization 阶段再次干预 AI 导致的崩溃风险。

#### 配置与日志

- 新增 `spawn_pal.max_requests_per_tick`，默认 `16`，用于控制每个游戏 Tick 最多处理多少个 `spawn_pal` 队列请求。
- 新增 `spawn_pal.request_cooldown_ms`，默认 `0`，用于控制两次 `spawn_pal` 请求之间的冷却时间。
- 新增 `spawn_pal.request_after_finalization_cooldown_ms`，默认 `0`，用于控制单只帕鲁完成最终确认后，下次 `spawn_pal` 请求的额外冷却。
- 上述配置会随 `config.json` 自动同步补齐，旧配置文件无需手动重建。
- 默认关闭 UE4SS Debug GUI 日志输出，避免 debug 级生成诊断刷屏；文件日志仍可按 `log.file_level` 保留 debug 信息。
- 调整配置加载顺序，先加载配置再初始化日志系统，使日志级别和输出开关能按配置生效。

#### 接入说明

- `spawn_pal` 现在是“要么生成带原生 AI 的世界帕鲁，要么失败”的行为，不再接受无 AI 成功作为 fallback。
- 如果需要高频批量生成，优先调整 `spawn_pal.max_requests_per_tick` 和两个 cooldown 配置，而不是在外部并发压测 HTTP 接口。
- 默认配置下已经移除人工延迟，`count=3` 会尽量在游戏线程队列中快速处理；实际出现时间仍取决于游戏线程 Tick 和引擎生成时序。
- 如果接口返回失败，请优先检查日志中的 spawner context、native delegate、IndividualHandleList 和 queued request 相关记录。
- 这一版没有重新引入等级上限突破，也没有恢复已移除的踢人接口。

#### 版本备注

- 当前对外同步版本：`v1.5.9`
- 这一版覆盖 2026-06-19 13:31 之后到 2026-06-20 01:19 的 `spawn_pal` 生成管线重构、配置化和版本号更新。

### v1.5.8 稳定性加固与接口行为收口版

- 更新定位：生产稳定性加固、接口真实行为收口与高风险实验能力回滚
- 适用对象：长期运行 PalServerBridge、频繁调用发奖/生成/背包/帕鲁接口、依赖外部后台或 Discord Bot 自动化操作的服主与开发者
- 更新阶段：稳定性与边界治理阶段

这一版不是单点功能版，而是对 1.5.7 之后暴露出的风险点做系统收口。重点是减少服务端崩溃、假成功、无边界输入和反射写入风险，优先修正逻辑；确实无法用逻辑修正的地方，再补严格边界校验。

#### 重要调整

- 回滚实验性的等级上限突破服务端支持。此前验证过程中发现 99 级以上经验、经验表、总经验溢出和客户端表现存在高风险，不作为本版正式能力发布。
- 移除 `/api/kick_player`。多条候选路径已验证无法可靠踢出玩家，继续保留会造成“接口返回成功但实际无效”的误导。
- 经验发放改为走游戏原生经验发放流程，成功判断以实际 Exp 变化为准，不再依赖直接写 Exp 字段或不可用的 Debug/Cheat 路径。
- `add_player_exp`、`add_party_exp`、`add_all_player_exp` 修复为可验证的实际生效路径。
- 世界生成帕鲁恢复游荡行为，避免生成后只站在原地、失去仇恨后停滞的问题。

#### 稳定性加固

- 大量反射调用改为校验函数签名、参数布局、返回值类型、数组字段、对象字段和写入范围后再执行。
- 补强 `FString`、数组、结构体、实例 ID、物品槽位、帕鲁槽位、工会/据点数据、玩家数据等反射读写边界。
- 函数参数生命周期改为 RAII 管理，减少临时参数、复制结构和失败路径释放不完整造成的崩溃风险。
- 移除不安全的固定偏移 fallback，优先使用可验证的反射字段和已有封装路径。
- 游戏线程任务增加超时取消、异常捕获、队列背压和缓存刷新合并，降低高频请求拖垮游戏线程的概率。
- HTTP 服务关闭顺序和 GameApi 关闭阶段加固，减少退出或重载时后台任务访问已释放对象的风险。
- 异步日志队列、聊天 hook 注销、更新公告下载大小、日志配置、配置文件解析等运行时边界补强。

#### 接口与模板修复

- 物品发放、物品删除、精确实例查找、动态物品 ID 重置、物品槽位清理和失败回滚逻辑进一步收口。
- 帕鲁发放使用唯一请求 ID，并复用网络个体解析路径，减少批量发放和重复请求下的冲突风险。
- 发放帕鲁前检查帕鲁仓库容量，避免容量不足时继续写入导致状态异常。
- 队伍帕鲁删除、帕鲁槽位元数据读取、Otomo 容器范围、召回相关反射调用继续加固。
- 模板执行补充递归深度、路径范围、数字有限性、整型范围、权重溢出和模板文件大小限制。
- 生成相关输入拒绝非有限坐标/数值，批量生成超时时停止后续批次，生成后 AI 修复队列增加边界限制。
- `/api/resources`、文档导出、模板导出、HTTP 请求体、玩家标识、API key、UTF-8 转换等输入输出边界补强。
- 修复生成的金钱模板引用问题，避免模板示例指向错误资源。

#### 接入说明

- 如果外部后台依赖踢人能力，请不要继续调用 `/api/kick_player`；该接口已移除，避免误判为成功。
- 经验接口现在以游戏原生流程和实际 Exp 增加为准，调用方应按返回结果判断是否生效。
- 大批量发放物品、帕鲁或世界生成时，仍建议控制频率；本版已经补充队列背压和批次中止，但不建议无限并发压测游戏线程。
- 等级上限突破不属于 1.5.8 正式能力，后续如果继续研究，应作为独立客户端/服务端协议能力重新设计。

#### 版本备注

- 当前对外同步版本：`v1.5.8`
- 这一版覆盖 1.5.7 之后到 2026-06-19 的稳定性修复、接口行为收口和高风险实验能力回滚。

### v1.5.7 spawn_pal 生成稳定性与诊断收口版

- 更新定位：世界生成帕鲁稳定性修复与批量生成链路收口
- 适用对象：使用 `/api/execute`、`spawn_pal`、商城批量发放/生成帕鲁、活动刷怪和自动化生成脚本的服主与开发者
- 更新阶段：生成链路稳定化阶段

这一版集中处理 `spawn_pal` 在真实服务器里暴露出的几个问题：批量生成时可能缺数量、部分生成请求首轮失败、普通世界帕鲁生成后 AI 初始化不稳定，以及大量连续生成操作集中压到同一游戏帧时带来的卡顿和崩溃风险。

#### 这版修复了什么

- `spawn_pal` 批量执行改为跨游戏线程步骤处理，避免大量帕鲁生成或模板操作一次性挤在同一帧执行。
- 批量 `spawn_pal` 会为每次请求生成 trace，记录批次开始、单次尝试、坐标、成功数量和完成状态，便于定位缺数量问题。
- 多只帕鲁生成时支持自动错位生成，降低多只实体堆叠在同一坐标造成卡地、遮挡或看似缺失的概率。
- 支持 `SpawnLocations` 指定每只帕鲁的独立生成坐标，也支持 `SpawnSpacing` 控制自动分散间距。
- 世界生成帕鲁改为通过网络个体注册路径创建稳定 IndividualID，再按该 ID 生成 Actor，减少短生命周期 handle 或全零实例 ID 导致的生成失败。
- 修复普通世界生成帕鲁 AI 初始化不稳定的问题，补齐野生行为所需的控制器、默认行为和出生点/游走范围信息。
- `spawn_pal` 支持透传 `WildAI`、`ReturnRadius`、`WalkRadius`、`WildLifeWalkingAreaRadius`、`ReturnRadiusRate_Combat`、`LeashType`、`LeashRadius` 等野生 AI 参数。
- 生成后的 AI 修复流程会在限定帧数内按 IndividualID 继续查找 handle/actor，降低首帧未解析到对象时直接失败的概率。
- 生成诊断日志从控制台刷屏降级为 debug 级别，保留排查能力的同时减少日常运行噪音。
- `/api/resources` 文档导出说明补充了 `language`、`include_all_languages` 和按语言返回文档的规则。

#### 接入说明

- 如果要生成多只帕鲁，推荐明确传入 `count`，并根据场景使用 `SpawnSpacing` 或 `SpawnLocations` 避免实体重叠。
- 如果需要模拟野生帕鲁游荡行为，可通过 `WildAI` 或相关半径参数调整出生点、游走半径和回归半径。
- `spawn_pal` 仍是“按坐标生成世界帕鲁”的能力，不等同于完整触发游戏内 Raid、入侵或祭坛系统。
- 调试生成问题时，请优先提供同一 trace 下的 batch begin、attempt result、SpawnPal request 和 AI fix 相关日志。

#### 版本备注

- 当前对外同步版本：`v1.5.7`
- 这一版覆盖 2026-06-07 版本号提交节点的 `spawn_pal` 稳定性修复。

### v1.5.6 更新公告静默拉取与一次性提示版

- 更新定位：更新公告拉取链路补强与静默轮询收口
- 适用对象：依赖自动更新提示、远程版本校验和后台静默运行的服主与开发者
- 更新阶段：公告检查静默化阶段

这一版从更新公告拉取链路开始补强。启动后先执行一次异步检查，随后再进入每半小时的静默定时检查；如果远程版本比本地新，只提示一次并停止后续拉取，直到下次重新启动插件才会再次进入检查周期。若已经是最新版本，或远程拉取失败，则保持静默，不额外刷屏。

#### 这版改了什么

- 启动后先走一次异步更新公告检查，用于尽早发现新版本。
- 异步检查完成后再进入每半小时的静默定时检查。
- 发现新版本时只输出一次提醒，随后停止后续周期检查。
- 如果已经是最新版本，或者远程更新源拉取失败，则不主动输出多余内容。
- 更新公告仍继续沿用公开 `UPDATE.md` 作为统一版本源。

#### 版本备注

- 当前对外同步版本：`v1.5.6`
- 这一版重点是更新公告检查行为收口，不新增业务 API。

### v1.5.5 spawn_pal AI 修复与开发者接入反馈整理版

- 更新定位：世界生成帕鲁可靠性修复与开发者接入说明补强
- 适用对象：使用 `/api/execute`、`spawn_pal`、玩家 GUID、物品扣除、外部后台和 Discord Bot 集成的开发者
- 更新阶段：生产反馈收口版

这一版主要收口 Anteros / ApexPals 两轮生产接入反馈，以及本地复测时暴露出的 `spawn_pal` 行为问题。重点不是堆新接口，而是让已经公开的接口行为更明确、更适合被外部系统长期依赖。

#### 兼容性与发布包调整

- 发布包继续配套较新的 UE4SS 版本，降低 PalServerBridge、PalSchema 与 UE4SS API 不一致带来的兼容风险。
- PalSchema 改为随发布包提供独立构建版本，不再依赖旧官方 PalSchema 所绑定的旧 UE4SS 假设。
- 发布包继续保持 UE4SS、PalServerBridge 和 PalSchema 成套分发，减少只更新部分文件导致的版本不匹配问题。

#### 这版修复了什么

- 修复 `spawn_pal` 生成出的普通帕鲁没有 AI、不会主动行动的问题。
- 调整 `spawn_pal` 的底层生成流程，避免部分环境下生成成功但后续 AI 初始化目标丢失。
- AI 修复诊断日志改为只写入日志文件，不再刷终端控制台或 UE4SS 控制台，方便排查同时减少控制台噪音。
- `/api/execute` 中的 `spawn_pal` 现在按世界坐标生成处理，不再要求 `steamid`。玩家上下文动作仍然需要 `steamid`。
- `/api/players` 保留原有 `player_uid` 无短横线格式，同时新增 `player_uid_dashed`，用于和引擎 `FGuid::ToString()` 或 Lua 事件里的 `8-8-8-8` GUID 对齐。
- `delete_item` 文档补充说明：接口走玩家库存数据路径，不是直接修改在线 Actor；离线写入是否能落到存档，取决于服务端能否通过目标玩家 UID 解析到库存对象。
- 修正 `World Spawn Pal Guide` 示例和说明，`CharacterID` 必须放在 `SaveParameter.CharacterID`，不能放在 `data` 根层。

#### 接入说明

- `spawn_pal` 只负责“在世界坐标生成帕鲁并修复普通 AI 初始化”，它不等同于触发游戏内真实 Raid、入侵或祭坛召唤事件。
- 真正的 Raid/Boss 事件还涉及 `BP_PalInvaderManager_C`、`UPalRaidBossAreaWorldSubsystem`、`UPalRaidBossManager` 等游戏内部系统，后续会作为独立研究方向处理。
- 对接外部系统时，推荐使用 `player_uid_dashed` 与 Lua 事件或引擎侧 GUID 做匹配，继续保留 `player_uid` 是为了兼容旧接入。

#### 版本备注

- 当前对外同步版本：`v1.5.5`
- 这一版包含来自 Anteros 反馈的文档修正和 API 行为整理，也包含本地确认后的 `spawn_pal` AI 修复。

### v1.5.4 UE4SS 升级与 PalSchema 独立构建版

- 更新定位：UE4SS 版本升级和 PalSchema 构建链重整
- 适用对象：需要同时维护 PalServerBridge、PalSchema 和 UE4SS 的构建维护者
- 更新阶段：兼容层收口与构建链统一阶段

这一版的核心变化只有两件事。

第一，UE4SS 切换到较新的 `RE-UE4SS-latest-mainline-e31aaaa`，让 `PalServerBridge` 和 `PalSchema` 都直接面向当前 UE4SS API 构建，减少旧版 UE4SS 带来的接口不匹配风险。

第二，`PalSchema` 不再依赖原官方 PalSchema 的旧构建假设，而是在本仓库内独立构建并直接接入新的 UE4SS 源树。这样可以把兼容性问题收口到当前代码库里，而不是继续沿用旧官方产物里的历史兼容层。

#### 这版改了什么

- `PalServerBridge` 和 `PalSchema` 统一改为使用较新的 UE4SS mainline 源树。
- `PalSchema` 改为在本仓库内独立构建，不再依赖旧版官方 PalSchema 的 UE4SS 兼容前提。
- 总构建脚本和发布打包流程继续跟随 `PalServerBridge` 版本号输出。
- 对外更新说明和实际构建结果保持一致，便于开发者按版本排查兼容性差异。

#### 版本备注

- 当前对外同步版本：`v1.5.4`
- 这一版重点是 UE4SS 升级和 PalSchema 构建链调整，不新增业务 API。

### v1.5.3 发布覆盖与崩溃排查增强版

- 更新定位：发布覆盖流程与跨机器排查继续补强
- 适用对象：服主、租户管理员、NexusMods 下载用户、自行构建并覆盖安装的管理员
- 更新阶段：发布可靠性与问题反馈定位阶段

这一版重点解决“用户只覆盖了 main.dll，UE4SS.dll 没有同步更新”带来的不兼容风险。PalServerBridge 当前发布包依赖配套的 UE4SS 版本，半套文件更新可能导致加载失败、启动闪退或崩溃信息不完整，所以构建后的部署流程现在会同步覆盖两者。

#### 这版改了什么

- 构建完成后覆盖到服务端目录时，会同步覆盖 `ue4ss/UE4SS.dll` 和 `ue4ss/Mods/PalServerBridge/dlls/main.dll`。
- 发布包继续保持 `UE4SS + PalServerBridge` 成套分发，降低用户只更新单个 DLL 的概率。
- 延续上一版的崩溃诊断增强，便于判断问题来自旧文件残留、杀软隔离、UE4SS 不匹配，还是插件运行时逻辑。
- 对外反馈问题时，请优先提供插件版本号、复现步骤、终端输出、`UE4SS.log`、PalServerBridge 日志和 dmp 下载地址。

#### 版本备注

- 当前对外同步版本：`v1.5.3`
- 这一版不新增业务 API，重点是降低发布覆盖错误和跨机器排障成本。

### v1.5.2 跨机器崩溃诊断与安装排查增强版

- 更新定位：不同机器闪退问题的诊断能力增强
- 适用对象：服主、租户管理员、NexusMods 下载用户、自行覆盖安装 DLL 的管理员
- 更新阶段：稳定性排查与发布验证阶段

这一版主要针对“本机稳定，但其他机器启动闪退或无法加载”的反馈补强诊断能力。它不把未知错误静默当成成功，而是在插件和 UE4SS 的关键边界留下更明确的异常信息，方便判断到底是安装覆盖不完整、杀软隔离、UE4SS 与 main.dll 不匹配，还是某个插件线程或游戏线程任务触发了非法指针。

#### 这版改了什么

- main.dll 增加结构化异常诊断保护，覆盖模块初始化、模块关闭、模块 Tick、游戏线程任务、HTTP 服务线程和更新公告线程。
- 崩溃诊断日志会记录异常码、访问类型、异常地址、RIP/RSP、当前 main.dll 路径、文件大小、修改时间和加载基址。
- UE4SS 侧 C++ Mod 生命周期边界也补充了崩溃防护，出现 C++ Mod 边界异常时会记录对应 mod、阶段和 main.dll 文件信息。
- 插件启动后会输出安装诊断信息，便于确认用户实际加载的是哪个 `main.dll`，而不是旧文件或被杀软隔离后的残缺目录。
- 发布包继续保持标准结构，`ue4ss/Mods/PalServerBridge/dlls/main.dll` 会随 ZIP 一起生成，方便排查“dlls 目录为空”这类安装问题。

#### 版本备注

- 当前对外同步版本：`v1.5.2`
- 这一版重点是诊断和稳定性边界增强，不新增新的业务 API。
- 如果仍然发生闪退，请同时提供 `UE4SS.log`、`PalServerBridge/logs/*.log`、`UE4SS.dll` 文件大小与修改时间、`main.dll` 文件大小与修改时间。

### v1.5.1 日志与发布打包优化版

- 更新定位：日志输出统一与发布打包流程补强
- 适用对象：服主、租户管理员、自行构建和发布 DLL 的管理员
- 更新阶段：运维体验与发布流程优化阶段

这一版主要收口了插件日志的三路输出格式，并把发布时需要手工整理文件的步骤下沉到构建流程里。重点不是继续扩业务接口，而是让排障、观察和对外发包更稳定、更省人工。

#### 这版改了什么

- 统一了日志输出规则，分别适配日志文件、UE4SS 控制台和游戏 CMD 控制台。
- UE4SS 控制台日志改为简写前缀 `PSB`，减少控制台横向占用。
- 游戏 CMD 控制台补上时间前缀，并对日志级别文本做颜色区分，便于快速判断问题级别。
- 构建完成后会自动生成标准发布 ZIP，减少手动拷贝 `dwmapi.dll`、`UE4SS.dll` 和模组目录的重复操作。
- 发布 ZIP 名称已与插件版本号联动，版本升级后可直接产出对应命名的发包文件。

#### 版本备注

- 当前对外同步版本：`v1.5.1`
- 这一版重点是日志体验和发布流程优化，不涉及新的业务接口扩展。

### v1.5.0 DLL 公告与公开更新源同步版

- 更新定位：DLL 公告与 PalServerBridge 公开更新源同步
- 适用对象：服主、租户管理员、查看 DLL 插件公告的管理员
- 更新阶段：对外公告统一发布阶段

这一版把后台项目里的 DLL 公告和 PalServerBridge 的公开更新源统一起来。以后插件发布的对外版本说明，会先同步到公开更新源，再同步到后台 DLL 公告，避免面板里看到的说明和插件实际发布内容不一致。

#### 主要同步

- 同步了 PalServerBridge 的对外版本号与更新说明口径。
- 后台项目 DLL 公告现在和插件公开更新源保持同一份发布文本。
- 后续 DLL 插件的版本发布会沿用统一公告源，减少重复维护。

#### 版本备注

- 当前对外同步版本：`v1.5.0`
- 这一版的重点是公告同步，不是新增新的插件功能。

### v1.4.1 队伍帕鲁状态同步修正版

- 更新定位：队伍帕鲁删除/寄存后的运行时状态同步补强
- 适用对象：服主、租户管理员、使用网页帕鲁仓库与回发能力的服务器
- 更新阶段：插件稳定性定点修复阶段

这一版是一次针对队伍帕鲁状态同步问题的定点修复。重点不是增加新接口，而是提升涉及寄存、删除和网页转移场景下的稳定性，减少后续操作异常对游戏服造成的影响。

#### 这版改了什么

- 优化了队伍帕鲁在寄存、删除和转移场景下的状态收口逻辑，相关操作完成后的后续行为更稳定。
- 补强了队伍槽位与当前帕鲁状态之间的同步流程，减少旧状态残留带来的异常动作。
- 修复了部分服务器在处理队伍帕鲁后，后续快捷键操作可能继续沿用旧状态的问题。
- 本次版本在整理后的干净基线上完成回收，后续继续维护和补强会更稳。

#### 版本备注

- 这一版适合已经启用网页帕鲁仓库、奖励暂存或相关转移能力的服务器优先更新。
- 如果你的服务器之前遇到过队伍帕鲁处理后后续动作异常，这一版就是针对该类问题的正式修正版本。

### v1.4.0 数据补充与新接口版

- 更新定位：数据补充与接口扩展阶段
- 适用对象：服主、租户管理员
- 更新阶段：背包与数据联动补强阶段

这一版继续往下补玩家、物品和帕鲁相关数据，同时把接口往背包联动需要的方向扩开。重点还是两件事：让面板拿到更多可用数据，让后面的联动逻辑少绕路。

#### 这版改了什么

- 玩家、物品、帕鲁相关数据继续补齐，面板侧可展示的信息更完整。
- 新接口继续补进来，背包、商城和玩家功能的联动空间更大。
- 数据读取和操作链路进一步往前推，后续接复杂玩法时不用再绕回头路。

#### 版本备注

- 这一版接的是前面接口扩展和性能整理后的继续补完。
- 从这里开始，插件侧已经不只是“能调接口”，而是在往完整的数据桥接层走。

### v1.3.0 性能与队列优化版

- 更新定位：高频调用场景稳定性增强
- 适用对象：服主、租户管理员
- 更新阶段：性能优化阶段

这一版的重点很明确，就是减少卡顿风险，让插件在高频请求和持续运行时更稳。

#### 主要优化

- 线程和任务队列逻辑继续优化，减少高频调用带来的阻塞风险。
- 插件在持续请求场景下的稳定性更好。
- 玩家数据补充了更多实用字段，便于面板展示。

#### 使用价值

- 当面板需要频繁读取玩家数据或连续执行操作时，插件更不容易拖慢游戏服。
- 对长期运行的租户场景更友好。

### v1.2.0 构建与资源导出版

- 更新定位：部署流程和资源支撑能力增强
- 适用对象：服主、租户管理员
- 更新阶段：维护配套补强阶段

这一版除了继续补接口，更重要的是把构建和资源导出能力补齐。对于长期维护面板和对照资源，这一版很关键。

#### 主要新增

- 构建流程继续调整，部署 DLL 更顺手。
- 资源导出文件补齐，便于后续维护物品、帕鲁等数据对照。
- HTTP 路由继续整理，插件对外提供能力更统一。

#### 使用价值

- 后续做后台对照表、图片映射和数据维护时更省力。
- 插件更新与重建时，日常维护成本更低。

### v1.1.0 接口与数据扩展版

- 更新定位：基础接口进入可持续扩展阶段
- 适用对象：服主、租户管理员
- 更新阶段：插件可用性增强阶段

这一版主要把插件从“有基础能力”推进到“更适合继续接面板功能”。除了接口增加，玩家和帕鲁相关数据也开始更完整。

#### 主要新增

- 玩家物品和帕鲁相关数据进一步补齐。
- 接口组织方式更清晰，后续继续加功能更方便。
- 测试工具继续完善，日常验证效率更高。
- 文档和接口说明进一步成型，减少接入时的反复试错。

#### 使用价值

- 面板在接入更多玩家数据时更顺手。
- 后续做物品、帕鲁、玩家资料展示时，插件侧可提供更稳定的数据基础。

### v1.0.0 DLL 桥接上线版

- 更新定位：首个可用版本
- 适用对象：服主、租户管理员
- 更新阶段：面板与游戏服直连能力上线阶段

这一版是 DLL 插件的起点版本。重点不是做界面，而是把面板和游戏服之间的直连通道先建立起来，让后续的发物品、查玩家、查数据都能走更直接的方式。

#### 主要新增

- 插件基础框架正式成型，可独立编译和部署。
- HTTP 通信能力上线，为面板调用游戏服功能提供统一入口。
- 在线玩家查询、发物品、发帕鲁、聊天等核心能力开始进入插件体系。
- 基础测试脚本和配置示例一并补齐，方便部署验证。

#### 使用价值

- 面板开始具备绕开传统控制方式、直接调用游戏内能力的基础。
- 后续商城、背包、玩家数据联动，都是从这一版开始往上扩展。
